import { InlineActionsPipe } from './inline-actions.pipe';

describe('InlineActionsPipe', () => {
  it('create an instance', () => {
    const pipe = new InlineActionsPipe();
    expect(pipe).toBeTruthy();
  });
});
