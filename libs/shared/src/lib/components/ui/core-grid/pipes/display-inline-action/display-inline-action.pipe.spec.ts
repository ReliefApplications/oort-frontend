import { DisplayInlineActionPipe } from './display-inline-action.pipe';

describe('DisplayInlineActionPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayInlineActionPipe();
    expect(pipe).toBeTruthy();
  });
});
