import { MeasureInlineActionsColumnWidthDirective } from './measure-inline-actions-column-width.directive';

describe('MeasureInlineActionsColumnWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new MeasureInlineActionsColumnWidthDirective();
    expect(directive).toBeTruthy();
  });
});
