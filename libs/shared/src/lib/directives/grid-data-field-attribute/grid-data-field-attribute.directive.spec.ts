import { GridDataFieldAttributeDirective } from './grid-data-field-attribute.directive';

describe('GridDataFieldAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new GridDataFieldAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
